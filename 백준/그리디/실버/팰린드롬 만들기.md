# 📖 백준 1213번) 팰린드롬 만들기
## 📌 문제 정의

임문빈은 임한수의 영어 이름으로 팰린드롬을 만들려고 하는데, 임한수의 영어 이름의 알파벳 순서를 적절히 바꿔서 팰린드롬을 만들려고 한다.
임문빈을 도와 임한수의 영어 이름을 팰린드롬으로 바꾸는 프로그램을 작성하시오.

### 입력
첫째 줄에 임한수의 영어 이름이 있다. 알파벳 대문자로만 된 최대 50글자이다.

### 출력
첫째 줄에 문제의 정답을 출력한다. 만약 불가능할 때는 "I'm Sorry Hansoo"를 출력한다.</br>
정답이 여러 개일 경우에는 사전순으로 앞서는 것을 출력한다.
</br>

<img width="799" height="526" alt="image" src="https://github.com/user-attachments/assets/5ea67f03-67ae-412e-aca5-e81bc9ec1a3d" />

</br>
</br>


## 📌 나의 풀이
### 📍최적화 코드
~~~python
#팰린드룸 : 앞으로 뒤로 읽어도 같은거

from collections import Counter
name = input()
arr_cnt = Counter(name)


odd_chr = []
for key, value in arr_cnt.items():
    if value % 2 == 1: # 문자 갯수가 홀수면 팰린드롬 불가할 수 있음
        odd_chr.append(key)


if len(odd_chr) > 1: # 홀수개인 문자가 1개면 팰린드 가능, 홀수개인 문자가 2개 이상이면 팰린드 불가 (ex. ABCBA는 가능, ABCDBA 불가능)
    print("I'm Sorry Hansoo")

else:
    if not odd_chr:
        mid = ""
    else:
        mid = odd_chr[0]

    half = []
    for key in sorted(arr_cnt.keys()):
        half.append(key * ((arr_cnt[key]) // 2))

    left = ''.join(half)
    right = ''.join(half[::-1])

    print(left + mid + right)
~~~
______________________________________________________
### ❗처음 구현한 코드 (permutations 사용) --> 시간 초과
~~~python
from itertools import permutations

name = input()
answer_lst = []

for i in permutations(name, len(name)):
    mid = len(i)//2
    true_cnt = 0

    for j in range(mid):
        if i[j] == i[(len(i)-1)-j]:
            true_cnt += 1

    if true_cnt == mid:
        if i not in answer_lst:
            answer_lst.append(i)

if not answer_lst:
    print("I'm Sorry Hansoo")

else:
    answer_lst.sort()
    print(''.join(answer_lst[0]))

~~~
</br>

## 💡문제 분석
- 팰린드롬은 앞으로 읽어도, 뒤로 읽어도 같은 문자열을 말함
- 정답이 여러개일 경우 사전 순으로 (정렬 패턴 필요)
- **Counter 함수 사용**

### 📖 Counter 함수
~~~python
from collections import Counter

arr = ['a', 'b', 'c']
arr_dict = Counter(arr)

for key, value in arr_dict.items(): #만약 key만 뽑고 싶으면 .keys(), value만 뽑으려면 .values()
     print(key, value)

print(arr_dict[key])
~~~
**출력 결과**
~~~
{a : 1, b : 1, c : 1}

arr_dict[key]를 하면 해당 key의 value 출력됨
~~~
