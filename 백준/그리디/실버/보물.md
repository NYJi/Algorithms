# 📖 백준 1026번) 보물
## 📌 문제 정의
옛날 옛적에 수학이 항상 큰 골칫거리였던 나라가 있었다. 이 나라의 국왕 김지민은 다음과 같은 문제를 내고 큰 상금을 걸었다.</br>
길이가 N인 정수 배열 A와 B가 있다. 다음과 같이 함수 S를 정의하자.

S = A[0] × B[0] + ... + A[N-1] × B[N-1] </br>

S의 값을 가장 작게 만들기 위해 A의 수를 재배열하자.
단, B에 있는 수는 재배열하면 안 된다.</br>S의 최솟값을 출력하는 프로그램을 작성하시오.

### 입력
첫째 줄에 N이 주어진다. 둘째 줄에는 A에 있는 N개의 수가 순서대로 주어지고, 셋째 줄에는 B에 있는 수가 순서대로 주어진다.</br>
N은 50보다 작거나 같은 자연수이고, A와 B의 각 원소는 100보다 작거나 같은 음이 아닌 정수이다.

### 출력
첫째 줄에 S의 최솟값을 출력한다.</br>

<img width="781" height="552" alt="image" src="https://github.com/user-attachments/assets/af6c7144-51e6-4b13-bbb5-d1240ef18b47" />
</br>

## 📌 나의 풀이
~~~python

n = int(input())
a_arr = list(map(int, input().split()))
b_arr = list(map(int, input().split()))

a_arr.sort()


#b_arr 움직이지 않고, 최솟값 만들기

s = 0

for i in range(n):
    s += a_arr[i] * max(b_arr)
    b_arr.remove(max(b_arr))

print(s)

~~~
</br>

### 💡문제 분석
- 최솟값을 만들기 위해서는 a_arr의 작은 수 * b_arr 큰 수를 서로 곱해주어야 한다.
-   곱했을 때, 최대한 작은 수가 나오기 위해서 (큰 수 * 작은 수)

❗**단, b_arr를 재배열하면 안되기 때문에 b_arr 리스트에서 max값을 찾아 곱해주고, 해당 리스트에서 삭제해주는 방식으로 작동**
~~~
b_arr= [3,5,10,7]
b_arr.remove(max(b_arr)

1회차) max(b_arr) = 10 --> [3,5,7]
2회차) max(b_arr) = 7 --> [3,5]
3회차) max(b_arr) = 5 --> [3]
4회차) max(b_arr) = 3 --> []
~~~
